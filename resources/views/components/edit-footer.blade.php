<div class="form-group mb-0 justify-content-start">
    <button type="button" class="btn btn-secondary">
        <a class="text-decoration-none text-light" href="{{ route('posts.index') }}">{{ __('Vissza') }}</a>
    </button>
    <button type="submit" class="btn btn-primary">{{ __('Mentés') }}</button>
</div>

